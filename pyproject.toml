[project]
name = "sarahs-research-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ringdown",
]

[tool.uv.sources]
ringdown = { git = "https://github.com/maxisi/ringdown.git" }
